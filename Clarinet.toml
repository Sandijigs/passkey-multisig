[project]
name = "passkey-multisig"
description = "Passkey-protected multisig wallet with biometric authentication"
authors = ["Stacks Builder"]
telemetry = false
cache_dir = "./.cache"
requirements = []

[contracts.passkey-multisig]
path = "contracts/passkey-multisig.clar"
clarity_version = 4
epoch = 3.3

[repl.analysis]
passes = ["check_checker"]
check_checker = { trusted_sender = false, trusted_caller = false, callee_filter = false }
